import React from 'react';

interface ARInterfaceProps {
  onClose: () => void;
}

const ARInterface: React.FC<ARInterfaceProps> = ({ onClose }) => {
  return <div>ARInterface</div>;
};

export default ARInterface;
